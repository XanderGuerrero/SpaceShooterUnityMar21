using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
    [SerializeField] private Transform PointsPopUp;
    // Start is called before the first frame update
    void Start()
    {
       // Transform PointsPopUpTransofm = Instantiate(PointsPopUp, Vector3.zero, Quaternion.identity);
        //DamagePopUp damagePopUp = PointsPopUpTransofm.GetComponent<DamagePopUp>();
       // damagePopUp.SetUpPopUp(2500,transform.position);
    }

  
}
